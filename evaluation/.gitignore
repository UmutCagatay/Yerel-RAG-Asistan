# Ölçüm çıktıları (sonuç dosyaları, raporlar) — versiyonlanmaz
results/
*.csv
*.html

# Sırlar — API key'ler asla repoya girmez
.env

# Python önbelleği / sanal ortam
__pycache__/
*.pyc
.venv/
venv/

# Not: degerlendirme_seti.json zaten kök .gitignore'daki *.json kuralıyla
# dışlanıyor (repoya gitmiyor). Set yerel ground truth olarak diskte durur.
